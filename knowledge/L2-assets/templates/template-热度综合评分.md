# 热度综合评分

- **领域**: devops
- **类型**: template
- **版本**: v1
- **来源项目**: ribbit-scanner
- **创建时间**: 2026-08-05T00:55:23.201340+00:00
- **最后更新**: 2026-08-05T00:55:23.201340+00:00
- **最后引用**: 2026-08-05T00:55:23.201340+00:00
- **权重**: 10
- **状态**: active
- **标签**: devops
- **关联知识**: 无

---

## 核心内容

### 模板说明

综合多维度指标计算热度评分 (0-100)

### 参数

`signal`

### 返回值

返回处理结果；具体值取决于输入和模板分支。

### 模板代码

```python
def compute_heat_score(signal):
    """综合多维度指标计算热度评分 (0-100)"""
    raw_heat = signal.get("upvotes", 0) * 0.3 + signal.get("comments", 0) * 0.4
    # 短文本衰减：低于30字的帖子热度减半
    if len(signal.get("content", "")) < 30:
        raw_heat *= 0.5
    # 趋势修正
    trend_score = signal.get("searchTrend", {}).get("score", 0)
    return min(100, round(raw_heat * 0.6 + trend_score * 0.4))
```

### 使用指导

调用 `compute_heat_score(signal)`，传入符合模板约定的参数，并根据返回结果继续业务处理。

### 注意事项

使用前应补充业务校验、异常处理和日志策略。

## 使用场景

适用于需要复用“热度综合评分”逻辑的项目。

## 前置条件

确认运行时版本、依赖包和输入数据结构与模板一致。

## 已知局限

使用前应补充业务校验、异常处理和日志策略。
