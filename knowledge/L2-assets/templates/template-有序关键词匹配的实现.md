# 有序关键词匹配的实现

- **领域**: devops
- **类型**: template
- **版本**: v1
- **来源项目**: ribbit-scanner
- **创建时间**: 2026-08-05T00:55:23.201340+00:00
- **最后更新**: 2026-08-05T00:55:23.201340+00:00
- **最后引用**: 2026-08-05T00:55:23.201340+00:00
- **权重**: 10
- **状态**: active
- **标签**: devops
- **关联知识**: 无

---

## 核心内容

### 模板说明

按优先级匹配，第一个命中即返回

### 参数

`text`

### 返回值

返回处理结果；具体值取决于输入和模板分支。

### 模板代码

```python
NEED_TYPES = [
    ("产品机会", ["想找", "有没有", "求推荐", "哪里能买到", "怎么实现"]),
    ("替代方案", ["替代", "竞品", "除了", "有没有类似", "换成"]),
    ("功能需求", ["希望", "如果能", "能不能", "加个功能", "想要"]),
    ("营销话题", ["爆火", "大家都在", "火了", "趋势", "风口"]),
    ("内容选题", ["怎么学", "教程", "入门", "新手", "经验分享"]),
    ("SEO词根", ["最好用的", "免费的", "在线", "工具", "网站"]),
    ("短期热闹", ["震惊", "太牛了", "笑死", "无语", "离谱"]),
]

def classify_need_type(text):
    """按优先级匹配，第一个命中即返回"""
    for need_type, keywords in NEED_TYPES:
        if any(kw in text for kw in keywords):
            return need_type
    return "其他"
```

### 使用指导

调用 `classify_need_type(text)`，传入符合模板约定的参数，并根据返回结果继续业务处理。

### 注意事项

使用前应补充业务校验、异常处理和日志策略。

## 使用场景

适用于需要复用“有序关键词匹配的实现”逻辑的项目。

## 前置条件

确认运行时版本、依赖包和输入数据结构与模板一致。

## 已知局限

使用前应补充业务校验、异常处理和日志策略。
